import { GlobalStyle } from "../../GlobalStyle";
import Header from "../../common/Header";
import { StyledHeader, StyledImages } from "./styled";

const NoResults = () => {
    <GlobalStyle>
        <StyledHeader>
            <Header />
            <StyledImages />
        </StyledHeader>
    </GlobalStyle>
};

export default NoResults;